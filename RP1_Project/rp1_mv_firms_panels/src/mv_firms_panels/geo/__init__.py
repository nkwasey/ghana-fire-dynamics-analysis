# file: src/mv_firms_panels/geo/__init__.py
"""Geospatial utilities (CRS alignment, masks, spatial joins, and QA helpers).

This subpackage is part of the public internal API used by pipeline stages.
It must exist as a standard package (not an implicit namespace) because the
project uses setuptools package discovery (find_packages).
"""
