# file: src/mv_firms_panels/version.py
"""Package version (single source of truth).

The version is read by packaging via `pyproject.toml` (setuptools dynamic version),
and should be updated here for releases.
"""

__version__ = "0.2.1"
